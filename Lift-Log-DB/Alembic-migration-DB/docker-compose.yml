services:
  postgres:
    image: postgres:17-alpine
    container_name: liftlog-migration-db
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: liftlog
    ports:
      - "5432:5432"
    tmpfs:
      - /var/lib/postgresql/data